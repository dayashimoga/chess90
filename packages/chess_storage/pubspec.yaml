name: chess_storage
description: Offline-first persistence for user profile, skill graph, games, review items, and backup export.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  chess_core:
    path: ../chess_core
  chess_learning:
    path: ../chess_learning
  chess_curriculum:
    path: ../chess_curriculum

dev_dependencies:
  test: ^1.24.0
  lints: ^3.0.0
