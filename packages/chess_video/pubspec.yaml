name: chess_video
description: "Deterministic chess video generator pipeline: timeline interpolation, frame calculation, overlays, and FFmpeg commands."
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  chess_core:
    path: ../chess_core
  chess_engine:
    path: ../chess_engine
  image: ^4.9.2

dev_dependencies:
  test: ^1.24.0
  lints: ^3.0.0
